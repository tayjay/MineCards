package com.tayjay.minecards.card;

import com.tayjay.minecards.reference.Names;
import net.minecraft.init.Blocks;

import java.util.ArrayList;
import java.util.List;

/**
 * Created by tayjm_000 on 2015-10-18.
 */
public class CardRegistry
{
    /*
    public static List<Card> cardsList = new ArrayList<Card>();
    public static Card stoneCard;
    public static Card grassCard;
    public static Card dirtCard;
    public static Card diamondPickaxeCard;
    public static int totalCards;

    public static void init()
    {
        registerCards();
        fillList();
    }

    public static void registerCards()
    {
        int id = -1;
        stoneCard = new Card(++id, Names.Cards.STONE_BLOCK,0,0);
        grassCard = new Card(++id,Names.Cards.GRASS_BLOCK,0,0);
        dirtCard = new Card(++id,"Dirt",0,0);
        diamondPickaxeCard = new Card(++id,"Diamond Pickaxe",3,1);
        totalCards = id;
    }

    public static void fillList()
    {
        cardsList.add(stoneCard.getId(),stoneCard);
        cardsList.add(grassCard.getId(),grassCard);
        cardsList.add(dirtCard.getId(),dirtCard);
        cardsList.add(diamondPickaxeCard.getId(),diamondPickaxeCard);
    }

    public static Card getCardById(int id)
    {
        return cardsList.get(id);
    }
    */
}
